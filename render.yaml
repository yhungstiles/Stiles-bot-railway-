services:
  - type: web
      name: trading-bot
          env: python
              buildCommand: |
                    pip install -r requirements.txt
                          playwright install --with-deps
                              startCommand: bash start.sh