services:
  - type: web
      name: trading-bot
          env: python
              buildCommand: |
                    pip install -r requirements.txt
                          playwright install chromium
                              startCommand: bash start.sh